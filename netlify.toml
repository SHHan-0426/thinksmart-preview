# 씽크스마트 v3 시험판 — Netlify 설정
# https://thinksmart-preview.netlify.app
#
# ⚠️ 폼 알림 이메일 설정 (배포 후 1회 수동)
# 1) https://app.netlify.com/projects/thinksmart-preview/forms
# 2) "event-registration" 폼 클릭
# 3) Settings → Form notifications → "Add notification" → Email notification
# 4) 이메일 주소: kty0651@hanmail.net
# 5) Event: New form submission
# 6) Save
# → 이후 신청 들어올 때마다 김태영 대표 이메일로 자동 발송

[build]
  # 정적 사이트라 빌드 명령 없음 — 루트 그대로 게시
  publish = "."

[build.processing]
  skip_processing = false

[build.processing.css]
  bundle = false
  minify = true

[build.processing.html]
  pretty_urls = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/ig_thumb/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/data/*.json"
  [headers.values]
    # etag로 매번 재검증(304면 캐시 사용) — 표지·인스타 업데이트 즉시 반영
    Cache-Control = "public, max-age=0, must-revalidate"

# 깔끔한 URL — /authors → /authors.html
[[redirects]]
  from = "/instagram"
  to = "/instagram.html"
  status = 200

[[redirects]]
  from = "/events"
  to = "/events.html"
  status = 200

[[redirects]]
  from = "/authors"
  to = "/authors.html"
  status = 200

[[redirects]]
  from = "/books"
  to = "/books.html"
  status = 200

[[redirects]]
  from = "/schools"
  to = "/schools.html"
  status = 200

[[redirects]]
  from = "/more"
  to = "/more.html"
  status = 200

# 내부 문서(docs) 비공개 — Git에는 보관하되 라이브 배포에서는 차단
# force=true 없으면 실제 파일이 그대로 서빙되어 차단이 무시됨
[[redirects]]
  from = "/docs/*"
  to = "/404.html"
  status = 404
  force = true
